# 디지털 추리 — 실시간 투표 연동 설치 가이드

교사들이 휴대폰으로 투표하면 → 강사 화면에 실시간 막대로 집계되는 구조입니다.
설치는 **10분**, 계정은 **본인 구글 계정 하나**면 됩니다. 무료·무설치.

---

## 0. 구성 한눈에
```
[교사 휴대폰] --QR스캔--> [투표 페이지]  ──기록──>  [구글 시트]
                              ▲                          │
                              │ "지금 이 질문 열기"       │ 집계
                              │                          ▼
                        [강사 화면(사건A.html)] <──JSONP── [집계 응답]
```
- 백엔드 = **Google Apps Script**(투표 페이지 서빙 + 기록 + 집계).
- QR 하나로 끝 — 화면이 넘어가면 **폰의 질문도 자동으로 바뀜**.
- 익명 — 이름·로그인 안 받음(기기별 임시 토큰으로 중복만 방지).

---

## 1. 백엔드 만들기 (Apps Script)

1. **구글 시트** 새로 만들기 (이름 예: `디지털추리_투표`).
2. 상단 메뉴 **확장 프로그램 → Apps Script**.
3. 기본 `Code.gs` 내용 **전부 지우고**, 파일 **`투표앱_AppsScript_Code.gs`** 내용을 **전체 복사·붙여넣기**.
4. 💾 저장(Ctrl+S).

## 2. 웹 앱으로 배포

1. 우측 상단 **배포 → 새 배포**.
2. ⚙️(유형 선택) → **웹 앱**.
3. 설정:
   - 설명: `디지털추리 투표`
   - 실행 계정: **나(본인)**
   - 액세스 권한: **모든 사용자** ← (꼭 이걸로. 로그인 없이 폰에서 접근 가능)
4. **배포** → 권한 검토/허용(본인 구글 계정으로 1회).
5. 표시되는 **웹 앱 URL**을 복사. 이렇게 생겼습니다:
   ```
   https://script.google.com/macros/s/AKfycb................/exec
   ```
   ⚠️ 끝이 **`/exec`** 인 주소가 맞습니다(`/dev` 아님).

> 코드를 수정하면 매번 **배포 → 배포 관리 → 편집(연필) → 버전 '새 버전' → 배포**로 갱신해야 반영됩니다.

## 3. 강사 화면에 URL 넣기

1. **`디지털추리_사건A_화면.html`** 을 편집기로 열기.
2. 위쪽 `<script>` 안의 설정 두 줄을 채웁니다:
   ```js
   var VOTE_API = "https://script.google.com/macros/s/AKfycb..../exec";  // 2단계에서 복사한 URL
   var ROOM     = "NOSUNG";   // 세션 코드(원하는 값, 영문/숫자 권장)
   ```
3. 저장. 끝.
   - `VOTE_API`가 **비어 있으면** 기존처럼 *데모 모드*('데모 결과 보기' 버튼)로 작동합니다.
   - 채우면 **라이브 모드** — 투표 단계에서 QR·실시간 결과가 자동으로 뜹니다.

---

## 4. 실제 강의 때 진행

1. 강사 화면(사건A)을 **전체화면**으로 열고 ▶ 사건 열기.
2. 투표 단계(STEP 2 / STEP 6)에 도달하면 화면에 **QR + 세션 코드**가 뜸.
3. 교사들이 폰 카메라로 **QR 스캔** → 투표 페이지 열림.
   - 화면이 투표 단계가 아닐 땐 폰에 *"다음 단서를 기다리는 중…"* 표시.
   - 투표 단계가 열리면 질문·선택지가 폰에 자동 등장 → 탭하면 제출(다시 탭해 변경 가능).
4. 강사 화면 하단 막대가 **2~3초마다 실시간 갱신**(`● LIVE · 참여 N명`).
5. 충분히 모이면 **다음 ▸** 으로 진행. (다음 단계로 가면 그 질문은 자동으로 닫힘)

> 💡 QR은 모든 질문에 **동일**합니다. 한 번 스캔하면 끝까지 그 페이지에서 따라옵니다.

---

## 5. 재시연·여러 반 운영

- **같은 반 다시 시연**: Apps Script 편집기에서 함수 `resetRoom` 선택 → ▶ 실행 (해당 세션 투표 삭제). 또는
- **다른 반/차시**: 화면의 `ROOM` 값을 다른 코드로 바꾸면 집계가 분리됩니다(`NOSUNG2` 등).
- 기록은 구글 시트 `Votes` 탭에 남습니다(timestamp·room·q·choice·token). 필요시 직접 삭제/분석 가능.

---

## 6. 사건 C·D로 확장 (예정)

투표 페이지(백엔드)는 **이미 C·D 질문까지 내장**돼 있습니다(`qText`에 C1~C3, D1~D3 포함).
사건 C·D 화면을 만들 때, 각 투표 패널에 아래만 넣으면 그대로 연동됩니다:
```html
<div class="vote" id="vote1" data-q="C1" data-opts="사춘기|우울|문제행동"> ... </div>
```
- `data-q`: 질문 ID(C1, C2, C3, D1…) — 백엔드 `qText`의 키와 일치
- `data-opts`: 선택지(`|`로 구분)
화면 상단 설정의 `VOTE_API`·`ROOM`은 동일하게 재사용.

---

## 7. 자주 막히는 곳 (체크)

| 증상 | 원인/해결 |
|---|---|
| 폰에서 페이지가 안 열림 | 배포 액세스가 **모든 사용자**인지 확인. `/exec` 주소 맞는지. |
| 결과 막대가 0에서 안 변함 | 화면 `VOTE_API` 오타 확인. 코드 수정했으면 **새 버전으로 재배포**했는지. |
| 폰이 계속 "기다리는 중" | 강사 화면이 투표 단계(STEP 2/6)인지. `ROOM` 값이 화면과 동일한지(대소문자 포함). |
| QR이 안 보임 | 강사 노트북이 인터넷 연결돼 있는지(QR 생성 라이브러리 로드). 안 되면 화면의 세션 코드로 주소 직접 안내. |
| 학교망에서 차단 | `script.google.com` 허용 필요. 보통 구글은 열려 있음. 막히면 모바일 데이터로 우회 가능. |

---

### 보안·개인정보
- 이름·학번·로그인 **수집 안 함**. 기기 임시 토큰(localStorage)으로 *중복 투표만* 방지.
- 투표 내용은 본인 구글 시트에만 저장. 외부 전송 없음.
- 액세스 "모든 사용자"는 *투표 페이지 접근*만 허용하는 것이며, 시트 자체는 본인만 봅니다.
